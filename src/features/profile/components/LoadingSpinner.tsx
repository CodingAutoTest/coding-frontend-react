import { FC } from 'react';

const LoadingSpinner: FC = () => {
  return (
    <div className="w-full h-full flex justify-center items-center text-gray-500">로딩 중...</div>
  );
};

export default LoadingSpinner;
